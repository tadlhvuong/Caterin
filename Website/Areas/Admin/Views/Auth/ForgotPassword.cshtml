@using Website.Areas.Admin.Models
@model ForgotPasswordViewModel
@{
    var culture = CultureInfo.CurrentCulture.Name;
}
<div class="card-body">
    <p class="login-box-msg" >ForgetPasswordTitleText</p>
    @Html.AntiForgeryToken()
    <div asp-validation-summary="All"></div>
    <form href="/admin/Account/ForgotPassword" method="post">
        <div class="input-group mb-3">
            <input asp-for="Email" type="email" class="form-control" placeholder="Email">
            <div class="input-group-append">
                <div class="input-group-text">
                    <span class="fas fa-envelope"></span>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <button type="submit" class="btn btn-primary btn-block" >ForgetPasswordBtnText</button>
            </div>
        </div>
    </form>
    <p class="mt-3 mb-1">
        <a href="/admin/auth/login">LoginText</a>
    </p>
</div>