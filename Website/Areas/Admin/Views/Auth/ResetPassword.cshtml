@* @using HKMain.Areas.Admin.Models
@model MemberResetPasswordModel
@{
    var culture = CultureInfo.CurrentCulture.Name;
    ViewData["Title"] = _localizer["ResetPasswordText"];
}
<div class="card-body">
    <p class="login-box-msg" >ResetPasswordTitleText</p>
    <form href="/admin/@culture/@_localizer["AccountUrl"]/@_localizer["ResetPasswordUrl"]" method="post">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="All"></div>
        <input asp-for="UserId" type="hidden" />
        <input asp-for="Code" type="hidden" />
        <div class="input-group mb-3">
            <input asp-for="Password" type="password" class="form-control" localize-att-placeholder="PasswordText">
            <div class="input-group-append">
                <div class="input-group-text">
                    <span class="fas fa-lock"></span>
                </div>
            </div>
        </div>
        <div class="input-group mb-3">
            <input asp-for="ConfirmPassword" type="password" class="form-control" localize-att-placeholder="RetypePasswordText">
            <div class="input-group-append">
                <div class="input-group-text">
                    <span class="fas fa-lock"></span>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <button type="submit" class="btn btn-primary btn-block" >ChangePasswordText</button>
            </div>
        </div>
    </form>

    <p class="mt-3 mb-1">
        <a href="/admin/@culture/@_localizer["AccountUrl"]/@_localizer["LoginUrl"]" >LoginText</a>
    </p>
</div> *@